{
  "name": "lighthouse-on-lambda-with-spreadsheets",
  "private": true,
  "version": "0.0.1",
  "description": "Run lighthouse audits from headless chrome in lambda. Write to Spreadsheets",
  "engines": {
    "node": ">= v10.18.1"
  },
  "scripts": {
    "deploy": "serverless deploy",
    "remove": "serverless remove",
    "postinstall": "cd layer/nodejs && yarn"
  },
  "author": "howdy39",
  "license": "MIT",
  "homepage": "https://github.com/howdy39/lighthouse-on-lambda",
  "dependencies": {
    "lighthouse": "^5.6.0",
    "request-promise": "^4.2.5"
  },
  "devDependencies": {
    "fs-extra": "^8.1.0",
    "prettier": "^1.18.2",
    "tar-fs": "^2.0.0"
  }
}
